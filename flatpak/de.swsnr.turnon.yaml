id: de.swsnr.turnon
runtime: org.gnome.Platform
runtime-version: "50"
sdk: org.gnome.Sdk
command: de.swsnr.turnon
finish-args:
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - pip-build.json
  - name: turnon
    buildsystem: simple
    sources:
      - type: git
        url: https://codeberg.org/swsnr/turnon.git
        tag: v3.0.0
        commit: 0875032e7fc8105cbf7a78016a7267ebe954ff86
    build-commands:
      # Crudely assert that we're not on a -dev version
      - grep version pyproject.toml | grep -qv -- '[.-]dev'
      - pip3 install --verbose --exists-action=i --no-index --no-deps --prefix=${FLATPAK_DEST} --no-build-isolation .
      - glib-compile-schemas /app/share/glib-2.0/schemas
